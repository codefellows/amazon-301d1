import React from 'react';
// We import React here even though we don't use the variable
// Because we have jsx on this page

const Footer = props => {
  return <footer>My Cool Footer</footer>;
};

export default Footer;
